@model Gateway.Storage.Dtos.UpdateAssignee

@{
    ViewData["Title"] = "Edytuj osobę przypisaną";
}

<div class="container mt-4">
    <h1 class="text-center">@ViewData["Title"]</h1>
    <hr />
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form asp-action="EditAssignee">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Assignee" class="control-label">Osoba przypisana</label>
                    <input asp-for="Assignee" class="form-control" />
                    <span asp-validation-for="Assignee" class="text-danger"></span>
                </div>
                <div class="form-group text-center">
                    <input type="submit" value="Zapisz" class="btn btn-primary" />
                </div>
            </form>
            <div class="text-center mt-4">
                <a class="btn btn-secondary" asp-action="Index">Cofnij</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
